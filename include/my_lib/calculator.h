#ifndef CALCULATOR_H
#define CALCULATOR_H

namespace my_lib{
    float add(float a, float b); // returns a+b
    float sub(float a, float b); // returns a-b
    float mul(float a, float b); // returns a*b
    float div(float a, float b); // returns a/b
} // namespace my_lib

#endif